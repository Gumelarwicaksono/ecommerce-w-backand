{
  "version": 2,
  "builds": [
    {
      "src": "server.js",  // Sekarang merujuk ke file root
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"  // Arahkan semua request ke index.js
    }
  ]
}
